-- Design

First, design the frontend UI.
Looking at the UI, design the database.
Looking at the UI and the database, design the backend.

-- Code

Code the frontend and the backend completely separately. Write tests that will not need the other one to work for both. Test them together as well.
Code the backend first.

-- Technologies

Design -> Figma
Database -> SQLite
Frontend -> mainly React
Backend -> Go
